# Exec Plan — N10 Print Library & Image Insert 기능 확장

> 이 문서는 살아있는 문서(living document)입니다.
> 작업 완료 시 `completed/` 폴더로 이동합니다.

---

## 개요
- **작업 유형**: 신규 기능 추가 (라이브러리 선택, 다중 업로드, 루트 이미지 풀 등)
- **대상 노드**: N10 Print (사이드바, 라이브러리 모달, 통합 API)

## 목표
- Library 모달의 삭제/체크박스 z-index 버그 수정
- 루트(폴더 밖) 이미지 풀을 별도로 관리·조회하는 로직 추가
- 라이브러리 모달에 `select` 모드를 주입 및 연동하여 파일 선택 UX 제공
- Sidebar 슬롯 클릭 시 [디바이스/라이브러리] 드롭다운 기능 도입

## User Review Required
진행 전 스펙 확정이 완료되었으므로, 이하 계획의 단계별 승인 후 곧바로 코드를 수정합니다.

## Progress

### 1단계: 삭제 로직 z-index 및 렌더링 버그 패치
- [x] 2026-04-16 — `LibraryModal.tsx` 내 폴더 삭제 체크박스가 폴더 본체(`z-10`)에 묻히는 문제를 `z-50` 레벨로 끌어올려 해결

### 2단계: API 및 라이브러리 데이터 구조 확장 (폴더 외 이미지 풀)
- [x] 2026-04-16 — `/api/library/route.ts` 수정: 디렉터리 내부 하위 폴더 탐색뿐 아니라 루트(최상단)에 있는 이미지 파일들도 수집
- [x] 2026-04-16 — 수집된 낱장 이미지들을 `[ALL IMAGES]` 혹은 루트 폴더 개념으로 `LibraryModal` 리스트에 함께 렌더링하도록 반영

### 3단계: 라이브러리 다중 모드 분리 (`manage` vs `select`)
- [x] 2026-04-16 — `LibraryModal.tsx`에 `mode: 'manage' | 'select'` 프롭스 추가
- [x] 2026-04-16 — `select` 모드: 모달 하단(또는 우단)에 `INSERT` 버튼 생성 / 상단 Add·Delete 아이콘 제거
- [x] 2026-04-16 — 폴더 클릭 이동 외에 체크박스 클릭 방식으로 전체/개별 다중 선택 기능 (VIDEO 모드 시 단일 강제)

### 4단계: `ImageInsert` 드롭다운 UI 연결
- [x] 2026-04-16 — `ImageInsert.tsx` 및 `VideoSlot.tsx` 버튼 클릭 시 네이티브 파일 다이얼로그 대신 커스텀 Dropdown 메뉴 표출
- [x] 2026-04-16 — 드롭다운 [디바이스 업로드] 클릭 시 네이티브 파일 다이얼로그 호출 연결
- [x] 2026-04-16 — 드롭다운 [라이브러리 업로드] 클릭 시 `LibraryModal`을 `select` 모드로 호출(`libraryActionTarget` 상태 사용)하여 선택된 이미지가 알맞은 슬롯에 이관되도록 처리

