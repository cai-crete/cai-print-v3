# Exec Plan — N10 print DXF 전용 이미지 전처리 및 4개 포맷 EXPORT

> 이 문서는 살아있는 문서(living document)입니다.
> 작업을 진행하면서 발견, 결정, 진행 상황을 이 문서에 지속적으로 업데이트합니다.
> 작업 완료 시 `completed/` 폴더로 이동합니다.

---

## 개요

- **작업 유형**: 새 기능 (DXF Export) 및 조건부 이미지 전처리
- **대상 노드**: N10 (print)
- **시작일**: 2026-04-20

---

## 목표

- `DRAWING` 모드에서 **PDF, JPG, PNG, DXF** 추출을 지원합니다.
- **포맷별 차별화 로직**:
    - **PDF, JPG, PNG**: 기존과 동일하게 템플릿 전체(텍스트, 라인 포함)를 전처리 없이 추출합니다.
- PDF, JPG, PNG: 기존과 동일하게 템플릿 전체(텍스트, 라인 포함)를 추출합니다.
- DXF: 텍스트 가독성과 선 정밀도(쭈글거림 방지)를 확보하면서 CAD 호환성 100%를 보장하는 추출 프로세스를 구축합니다.

---

## 트러블슈팅 및 해결 과정 히스토리 (v1 ~ v8)

변환 품질과 CAD 호환성을 모두 잡기 위해 총 8단계의 기술적 시행착오와 고도화 과정을 거쳤습니다.

| 버전 | 접근 방식 | 결과 및 한계 |
| :-- | :--- | :--- |
| **v1~v4** | 클라이언트 사이드 픽셀 전처리 | 이미지 배경은 제거되었으나, 픽셀 노이즈로 인해 **선이 쭈글거림**. |
| **v5** | jsPDF 기반 벡터 PDF 변환 | 텍스트 가독성은 완벽하나, **CAD 스케일 및 좌표계 불일치** 발생. |
| **v6** | 클라이언트 미세 블러 스무딩 | 선은 부드러워졌으나, 얇은 선이 블러에 뭉개져 **연결성이 끊김**. |
| **v7** | **Native SVG 좌표 드로잉** | 직선은 완벽하나, **이미지 유실 및 텍스트 깨짐** 발생 (변환기 한계). |
| **v8** | **하이브리드 안정화 (최종)** | **PNG 회귀 + 서버 sharp 고도화**. 안정성과 품질 모두 확보. |

---

## 상세 계획 및 단계별 요약

### 1단계: 환경 설정 및 라이브러리 설치
- `sharp` 라이브러리 설치 완료 및 확인.
- `lib/types.ts`의 `ExportFormat` 정의 확인.

### 2단계: UI 연동 (ActionButtons)
- `ActionButtons.tsx`에서 `DRAWING` 모드일 때 4가지 확장명(PDF, JPG, PNG, DXF)이 모두 드롭다운에 표시되도록 수정합니다.

### 3단계: 클라이언트 사이드 DXF 전용 전처리 로직 구현 (`export.ts`)
- `exportDocument` 함수 수정:
    - `format === 'dxf'` 인 경우:
        1. 캡처용 히든 iframe 내의 이미지 요소를 찾습니다.
        2. 해당 이미지를 서버(`/api/preprocess`)로 보내 Thresholding된 결과(Base64/Blob)를 받거나, 클라이언트 Canvas에서 직접 처리합니다. (서버 처리가 더 고품질일 수 있으므로 전처리용 API 활용 고려)
        3. 전처리된 이미지로 원본을 교체한 상태에서 `html2canvas` 전체 캡처를 수행합니다.
        4. 결과 결과물을 서버의 `/api/convert`로 전송합니다.

### 4단계: 서버 사이드 API 구현
- **`/api/convert/route.ts`**:
    - 클라이언트에서 이미 전처리된 이미지가 포함된 도면 캡처본(JPG/PDF)을 수신합니다.
    - 이를 Convertio API에 전달하여 DXF로 최종 변환 및 반환합니다.
- **`/api/preprocess/route.ts` (선택 사항)**:
    - 이미지 부분만 따로 던져서 고품질 Thresholding을 수행하는 전처리 전용 API입니다.

### 5단계: 검증 및 테스트
- PDF/JPG/PNG 추출 시 원본 품질이 유지되는지 확인합니다.
- DXF 추출 시 배경 패턴이 제거된 선명한 도면이 포함되는지 확인합니다.

---

## Progress

- [x] 2026-04-20 — UI 및 export 로직 수정 착수
- [x] 2026-04-20 — 텍스트 가독성 개선 (jsPDF 벡터 방식 시도)
- [x] 2026-04-20 — CAD 호환성 이슈 해결 (PNG 회귀 및 서버 고도화 결정)
- [x] 2026-04-20 — 서버 사이드 `sharp` 이미지 엔진 구축 및 최종 테스트 완료

---

## Surprises & Discoveries

- **SVG 변환기의 한계**: 대부분의 범용 변환기(Convertio 포함)가 SVG 내의 base64 이미지나 텍스트 인코딩을 DXF로 100% 옮기지 못함을 발견. (이미지 유실의 원인)
- **서버 사이드 처리의 우수성**: 클라이언트 브라우저 필터보다 서버 사이드 `sharp` 엔진의 `median(3)` 필터가 선의 쭈글거림(Jitter)을 잡는 데 훨씬 효과적임을 확인.
- **스케일 호환성**: 이미지 기반 변환이 가장 확실하게 CAD 스케일(1:1)을 유지시켜줌.

---

## Decision Log

| 날짜 | 결정 | 이유 |
|------|------|------|
| 2026-04-20 | DXF 전용 부분 전처리 도입 | JPG/PNG/PDF는 원본 템플릿 디자인 보존이 중요하며, DXF는 CAD 호환성을 위한 선 추출이 핵심이기 때문 |
| 2026-04-20 | 임계값 고정 처리 | 사용자 요청에 따라 별도의 UI 없이 최적의 고정값 적용 |
| 2026-04-20 | **서버 사이드 가공 방식 채택** | 클라이언트 성능 한계 및 쭈글거림 현상을 해결하기 위해 전문 엔진(`sharp`) 도입 결정 |
| 2026-04-20 | **Native SVG 방식 기각** | 이미지 유실 및 텍스트 렌더링 오류로 인해 안정성이 높은 PNG 방식으로 최종 회귀 결정 |

---

## 위험성 및 대책

- **이미지 교체 시점**: `html2canvas` 호출 전 iframe 내 이미지 로딩이 완료되어야 합니다. `waitForIframeLoad` 로직을 강화합니다.
- **Convertio 변환 한계**: 이미지 기반 DXF 변환 시 텍스트가 벡터가 아닌 패스로 변환될 수 있습니다. 고해상도 캡처를 통해 가독성을 확보합니다.

---

## Outcomes & Retrospective

- **원래 목표 달성 여부**: [x] Yes  [ ] Partial  [ ] No
- **결과 요약**: 
    1. `DRAWING` 모드에서 DXF 포함 4개 포맷 추출 기능 구현 완료.
    2. 서버 사이드 `sharp` 엔진 연동을 통해 선의 쭈글거림 현상 획기적 개선.
    3. 고해상도 PNG 캡처 기반으로 이미지 유실 및 텍스트 깨짐 현상 완벽 방어.
    4. CAD에서의 1:1 스케일 및 레이아웃 안정성 확보.
- **다음 작업에 반영할 것**: 
    - 향후 다른 노드에서도 도면 추출이 필요할 경우, 클라이언트 전처리보다는 서버 사이드 이미지 고도화 파이프라인(`sharp`)을 우선 고려할 것.

---

`COPYRIGHTS 2026. CRE-TE CO.,LTD. ALL RIGHTS RESERVED.`
